# Lumen v2 — additional dependencies (Magentic-One / autogen).
#
# Kept SEPARATE from the root requirements.txt on purpose: the ground rules for
# v2 forbid modifying any file outside /v2, and isolating these deps means a v1
# redeploy never accidentally pulls autogen. Install with:
#     pip install -r v2/requirements.txt
#
# Conflict check (run on 2026-06-11, Python 3.12): `pip install --dry-run` showed
# NO downgrades of existing v1 packages — openai 2.30, pydantic 2.12, fastapi,
# azure-identity, azure-cosmos, httpx all already satisfied autogen's ranges.
# autogen-core pins protobuf<6, which downgraded protobuf 6.33.5 -> 5.29.6; this
# is SAFE — `pip check` reports no broken requirements and nothing in v1 pins
# protobuf>=6. tiktoken was upgraded 0.7->0.13 (within v1's >=0.7 spec).

autogen-agentchat>=0.7.5,<0.8     # MagenticOneGroupChat (the Magentic-One orchestrator)
autogen-ext[openai]>=0.7.5,<0.8   # AzureOpenAIChatCompletionClient + autogen-core

# NOTE on the magentic-one extra:
# The user spec listed `autogen-ext[magentic-one,openai]`. The `[magentic-one]`
# extra exists only to pull MultimodalWebSurfer / FileSurfer / Coder + their heavy
# deps (playwright, markitdown[all], onnxruntime, pandas, ~hundreds of MB). Lumen
# v2 brings its OWN specialists (the v1 agents) and does not use those surfers, so
# we install the lean `[openai]` extra instead. If you ever want the built-in
# surfers, add:  autogen-ext[magentic-one]>=0.7.5,<0.8
